<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Application.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RailRadar"
        android:usesCleartextTraffic="true">

        <activity android:name=".HomeActivity.Home_Screen_Activity"
            android:exported="true">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>


        <activity android:name=".UserRouteTrains.User_Route_Train_List" />
        <activity android:name=".TrainNumber_NameSearch.Show_Trains"/>
        <activity android:name=".ExtraOptions.NearBy_Station_Activity" />
        <activity android:name=".TrainTracking.Train_Tracking" />

        <service android:name=".Services.LocationService.myLocationServiceClass"
            android:exported="false"
            android:foregroundServiceType="location"
            />
        <service android:name=".Services.API_Call.Train_Tracking_API_Call"/>
        <service android:name=".Services.API_Call.PNR_Enquiry_API_CALL" />
    </application>

</manifest>